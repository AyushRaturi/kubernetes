apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: NodePort
  selector:
    app: nginx   # MUST match pod label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007

