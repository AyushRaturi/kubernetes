kind: Jobs
apiVersion: v1
metadata:
  name: nginx-job
  namespace: nginx
spec:
  completions:
    parallelims:
  template:
    metadate:
      conatiner: :nginx

    spec:
      container:
        - name: nginx
          image: nginx:latest

